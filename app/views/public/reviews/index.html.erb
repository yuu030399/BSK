<% if @review.errors.any? %>
  <%= @review.errors.count %>errors prohibited this obj from being saved:
  <ul>
    <% @review.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

  <div class="container">
  <div class="row">
    <div class="col-md-3">


</div>
<div class="col-md-8 offset-md-1">
  <h2>reviews</h2>

  <table class="table table-hover table-inverse">

  <thead>
      <th>商品名</th>
      <th>金額</th>
      <th>ブランド名</th>
      <th colspan="3"></th>
  </thead>
<tbody>
  <% @reviews.each do |review| %>
  <tr>
   <%#= link_to image_tag(@user.get_profile_image(100, 100)), review.user %>

<%#= link_to review.product_name, review_path(review.id)%>
  <td><%= link_to review.product_name, review_path(review.id)%> </td>
  <td><%=review.amount  %></td>
　<td><%=review.category.brand_name%></td>
   <td> <%= link_to "Edit", edit_review_path(review.id) %></td>
   <td> <%= link_to "Destroy", review_path(review.id), method: :delete, "data-confirm" => "本当に消しますか？" %></td>
<% end %>
</tr>
</tbody>
</table>




 <%=link_to "投稿フォームへ",new_review_path%>
</div>
</div>
</div>
</html>
